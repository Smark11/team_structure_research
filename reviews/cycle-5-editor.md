# Cycle 5 — Reviewer C (editor), sign-off check

Scope: verification of C4-1…C4-6 only, by Python text scans over the served `docs/*.html` (tag-stripped and raw) and `decision-memo.md`, plus a sense re-read of each edited paragraph. No new scope opened.

## Verification table

| # | Claimed fix | Verified? | Served text |
|---|---|---|---|
| C4-1 | §4.5 cost numbers → memo values; index §1.1 agrees | **Substance yes — with a blocking splice residue (C5-1)** | Run-rate row: "+$1.7–3.5M a year against a $2.4M MSA-offshore mix; +$0.1–1.9M against a $4.0M boutique mix — the subtraction of the two columns, nothing else" ✓. CFO sentence: "on the assumed baseline this costs $1.7–3.5M a year more than an MSA-offshore vendor mix; the offset scenario removes $1.1–1.8M of that … against a boutique-heavy mix the base case is +$0.1–1.9M…" ✓. T12 subtotal: "T12 path, from its own lines — no conversion grants, ~40-hire recruiting, 12–18-month double-running: ≈ $5.0–6.3M plus two engineer-quarters for runbooks" ✓. Old numbers ("+$1.5–3.0M", "+$0.5–1.5M", "up to $6.8M") gone. Index §1.1: "$1.7–3.5M a year more … removing $1.1–1.8M … $1.6–4.4M one-time" ✓; index unwind "$1.5–3M" correctly untouched. **But** the old CFO sentence's tail was not fully deleted — see C5-1. |
| C4-2 | §4.3 "months 3–4 at best, modal 4–6"; G1 "at or after G0+6" | **Fixed** | Ownership-table caption: "Phases are offsets from G0 (Gate 0, target months 3–4 at best, modal 4–6) and G1 (the first fiscal-quarter boundary at or after G0+6); best-case months in parentheses." ✓. The §4.5 critical-path line no longer restates a G1 definition at all ("Two critical paths. To Gate 1: site head → engineering managers → Gate 1…") — zero occurrences of "target month 3", "first quarter boundary after", or any bare "after G0+6" remain; the one G1 definition on the page is the correct one. |
| C4-3 | §5.5 gate purpose + backfill pointer stated once each | **Substance yes — with a blocking splice residue (C5-2)** | "Regretted departures are a subset of the 18–25% gross rate; the gate detects a vendor-controlled or under-banded transfer, and the backfill line (Applying It §4.5) absorbs normal churn." — "the gate detects" now appears exactly once; the duplicated closing sentence is gone. **But** the deletion cut through the old sentence's anchor tag — see C5-2. |
| C4-4 | Conway quoted once on Foundations | **Fixed** | "constrained to produce designs" appears exactly once on `docs/foundations.html`, in the styled pull: "…organizations which design systems (in the broad sense used here) are constrained to produce designs which are copies of the communication structures of these organizations. — Melvin Conway, 1968". The plain duplicate blockquote is deleted; the two remaining plain blockquotes on the page are different quotes (the "physical office was covering up" quote and Urwick 1956). §3.4 opens cleanly: pull, then "Two things get forgotten about the 1968 paper…". |
| C4-5 | Zero "happy path" site-wide and in the memo | **Fixed** | Case-insensitive scan: 0 hits in all seven `docs/*.html` and `decision-memo.md`. Fig 1.2 SVG row label now "Best case" (calendar rows "Best case 3 9 18 30 / T1 path 6 12 21 33") ✓; alt summary "two calendar rows underneath for the best case and the T1 path" ✓, matching §1.3 prose ("the best case and the tripwire-T1 path"). Learning Plan ×4 ("the Recommendation's best case", "Month (best case)", "month 6 on the best case", "One month per row on the best case"), Charter §5.5 ("months 18 and 30 on the best-case calendar"), Applying It caption ("best-case months in parentheses") — all read sensibly. |
| C4-6 | "decided at Gate 2" capitalized | **Fixed** | Fig 1.2 swimlane: "decided at Gate 2" (1 hit); zero lowercase "gate <digit>" anywhere in `docs/index.html`. |

## New items — splice residue from the cycle-4 edits

**C5-1 — blocking.** `docs/applying-it.html`, §4.5 CFO paragraph: the new sentence was pasted over the front of the old one, but the old sentence's tail was left behind. The paragraph now reads: "…if the released roster shows otherwise, the conversion cost and the acceptance gate both move**.5–1.5M; against a boutique-heavy mix, closer to neutral.** It buys ownership, tenure, in-region on-call…". The orphaned fragment ".5–1.5M; against a boutique-heavy mix, closer to neutral." is visible garbage and — worse — reintroduces the retired "closer to neutral" claim two clauses after the correct "+$0.1–1.9M". Edit: delete the fragment so the text reads "…the conversion cost and the acceptance gate both move. It buys ownership, tenure…".

**C5-2 — blocking.** `docs/charter-evidence.html`, §5.5: the duplicate-sentence deletion cut through the old sentence's link markup. Raw HTML now reads: `…absorbs normal churn.html”>Applying It</a> §4.5).</p>` — the page visibly renders "…absorbs normal churn.html”>Applying It §4.5)." with junk text and an unmatched `</a>`. Edit: delete everything between "absorbs normal churn." and `</p>` (i.e., remove `html”>Applying It</a> §4.5).`).

Both fixes are one-line deletions; nothing else on either page needs to move. All other edited paragraphs re-read clean — no other orphaned clauses found by the splice sweep (patterns `”>`, `href=`, `.html”`, and mid-word number fragments across all seven pages returned only these two hits).

## Verdict

**Not zero blocking.** All six cycle-4 items are fixed in substance — every target number, phrase, label and capital verified against the memo — but two of the fixes left splice residue in the served HTML: an orphaned half-sentence in Applying It §4.5's CFO paragraph (C5-1) and broken anchor-tag text in Charter Evidence §5.5 (C5-2). Two one-line deletions and a rebuild should make cycle 6 a formality.
