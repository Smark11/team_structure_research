# Cycle 6 — Reviewer C (editor), formality check on C5-1/C5-2

1. **C5-1 verified fixed.** `docs/applying-it.html` §4.5 CFO paragraph reads "…the conversion cost and the acceptance gate both move. It buys ownership, tenure, in-region on-call…" — exactly one space between "move." and "It buys", no digit-run residue, and zero occurrences of "closer to neutral" anywhere on the page.
2. **C5-2 markup fixed, but the repair reopened C4-3 (new item C6-1, blocking).** `docs/charter-evidence.html` §5.5 now ends `…absorbs normal churn (<a href="applying-it.html">Applying It</a> §4.5).</p>` — anchor well-formed, junk text gone. However, instead of the prescribed deletion the fixer reconstructed the old anchor, so the sentence now cites the pointer twice: "…the backfill line (Applying It §4.5) absorbs normal churn (Applying It §4.5)." — the same reference twice in one sentence (first plain text, second linked), a regression of C4-3's "backfill pointer stated once". Edit: delete the trailing " (<a href=\"applying-it.html\">Applying It</a> §4.5)" so the sentence ends "…absorbs normal churn." (or, equivalently, keep the linked tail and delete the earlier plain "(Applying It §4.5)" — one copy, not two).
3. **Splice sweep clean.** All seven `docs/*.html` scanned for lowercase-word glued to `.html">` outside an href/src attribute (0 true hits; 30 raw matches all confirmed as tails of legitimate external-URL href values), letter-glued-to-digit-run in rendered text (all hits are DOI paths in displayed source URLs, e.g. `10.1287/mnsc.26.3.322`), `</a>` outside any open `<a>` (0), unclosed `<a>` (0), curly-quote residue `”>` (0), and camel-fused words across deleted regions (0 beyond proper-noun whitelist).

## Verdict

**Not zero blocking.** One item, C6-1: the C5-2 repair restored the deleted anchor instead of removing the residue, leaving "(Applying It §4.5)" stated twice in the same §5.5 sentence. One one-clause deletion and a rebuild; C5-1 and the site-wide splice sweep are otherwise fully clean.
